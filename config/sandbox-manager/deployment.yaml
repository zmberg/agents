apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-manager
  namespace: sandbox-system
  labels:
    component: sandbox-manager
    app.kubernetes.io/name: sandbox-manager
    app.kubernetes.io/managed-by: kustomize
spec:
  replicas: 1
  selector:
    matchLabels:
      component: sandbox-manager
      app.kubernetes.io/name: sandbox-manager
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: controller
      labels:
        component: sandbox-manager
        app.kubernetes.io/name: sandbox-manager
    spec:
      serviceAccountName: sandbox-manager
      containers:
        - name: controller
          image: sandbox-manager:latest
          imagePullPolicy: IfNotPresent
          command:
            - /sandbox-manager
          args:
            - -v=7
            - --zap-log-level=7
            - --system-namespace=sandbox-system
            - --peer-selector=component=sandbox-manager
            - --kube-client-qps=10000
            - --kube-client-burst=30000
            - --e2b-admin-key=some-api-key
            - --e2b-enable-auth=true
            - --e2b-max-timeout=2592000
            # - --quota-redis-addr=redis.sandbox-system.svc:6379
            # - --quota-redis-operation-timeout=50ms
            # - --quota-anti-drift-interval=5m
            # - --quota-anti-drift-grace=10m
            # OpenTelemetry tracing (uncomment and configure to enable)
            # - --tracing-mode=otel
            # - --tracing-endpoint=otel-collector:4317
            # - --tracing-insecure=false
          ports:
            - containerPort: 8080
              name: http-manager
            - containerPort: 9002
              name: grpc-ext-proc
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 7789
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            tcpSocket:
              port: 7789
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 30
            timeoutSeconds: 5
          env:
            - name: INFRA
              value: sandbox-cr
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            # - name: QUOTA_REDIS_USERNAME
            #   valueFrom:
            #     secretKeyRef:
            #       name: quota-redis
            #       key: username
            # - name: QUOTA_REDIS_PASSWORD
            #   valueFrom:
            #     secretKeyRef:
            #       name: quota-redis
            #       key: password
          resources:
            limits:
              cpu: "1"
              memory: "2Gi"
            requests:
              cpu: "500m"
              memory: "1Gi"
        - name: envoy-proxy
          image: envoyproxy/envoy:v1.33-latest
          imagePullPolicy: IfNotPresent
          args:
            - "--config-path /etc/envoy/envoy.yaml"
          livenessProbe:
            tcpSocket:
              port: 7788
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 7788
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            tcpSocket:
              port: 7788
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 30
            timeoutSeconds: 5
          ports:
            - containerPort: 7788
          volumeMounts:
            - name: envoy-config
              mountPath: /etc/envoy/envoy.yaml
              subPath: envoy.yaml
          resources:
            limits:
              cpu: "1"
              memory: "2Gi"
            requests:
              cpu: "500m"
              memory: "1Gi"
      volumes:
        - name: envoy-config
          configMap:
            name: sandbox-manager-envoy-config
